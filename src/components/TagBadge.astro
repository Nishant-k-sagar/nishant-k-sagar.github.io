---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a
  href={`/tags/${tag.toLowerCase()}`}
  class="inline-block px-3 py-1 text-xs font-medium bg-muted/20 text-muted rounded-full hover:bg-accent/10 hover:text-accent transition-colors"
>
  {tag}
</a>
